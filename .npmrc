# Tree-sitter grammar packages track the core ABI on different release lines.
# Keep the pinned grammar set installable from the lockfile without flattening
# incompatible optional peer ranges into a false package-resolution failure.
legacy-peer-deps=true
